{% extends '@projects/add.twig' %}

{% block toolbar %}
    <h2 data-toggle="tooltip" data-placement="bottom" data-original="{{ project.title | title }}" title="{{ environment.translate('Click to rename project and hit [Enter]') }}" contenteditable="">{{ project.title | title }}</h2>

    <div class="layer" style="border-bottom: 1px solid #ccc; padding: 0 30px 0 10px;">
        <button class="admin-nav-button active first" data-block="main" id="project-setting-main">
            <i class="fa fa-home"></i>
            {{ environment.translate('Main') }}
        </button>
        <button class="admin-nav-button" data-block="design" id="project-setting-design">
            <i class="fa fa-desktop"></i>
            {{ environment.translate('Design') }}
        </button>
        {#<button class="admin-nav-button" data-block="effects">
            <i class="fa fa-gear"></i>
            {{ environment.translate('Effects') }}
        </button>#}
        <button class="admin-nav-button" data-block="statistic">
            <i class="fa fa-line-chart"></i>
            {{ environment.translate('Statistic') }}
        </button>
        <!-- /.button -->
    </div>

    <div class="layer" style="float: right; margin-top: 8px;">
        <button class="button" id="save">
            <i class="fa fa-save"></i>
            {{ environment.translate('Save') }}
        </button>
        <!-- /.button -->
        <a href="{{ environment.generateUrl('projects', 'clone', { 'id': project.id }) }}" class="button">
            <i class="fa fa-copy"></i>
            {{ environment.translate('Clone') }}
        </a>
        {#<a href="{{ environment.generateUrl('shares', 'statistic', { 'project_id': project.id }) }}" class="button">
            <i class="fa fa-line-chart"></i>
            {{ environment.translate('Statistic') }}
        </a>#}
        <a href="{{ environment.generateUrl('projects', 'delete', { 'id': project.id }) }}" class="button delete">
            <i class="fa fa-trash"></i>
            {{ environment.translate('Delete') }}
        </a>
        <!-- /.button -->
    </div>
    <!-- /.layer -->

{% endblock %}

{% block sidebarTop %}
    {#<button id="top-sidebar-dialog" class="button button-primary"
            data-toggle="tooltip"
            data-original-title="Open sidebar customization dialog available in PRO"
            style="font-size: 10px; border-radius: 20px !important; height: 20px; line-height: 20px !important; margin-top: 1px;" disabled>
        <i class="fa fa-eye"></i>
        {{ environment.translate('Get Pro') }}
    </button>#}
{% endblock %}